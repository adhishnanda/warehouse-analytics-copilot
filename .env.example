# Groq (free tier) — used for development and iteration
GROQ_API_KEY=

# Ollama — alternative local dev backend, no key needed if running locally
OLLAMA_BASE_URL=http://localhost:11434

# Paid model — used only for final, reproducible evaluation runs in Week 2
# Flagged before use, never for routine development
EVAL_MODEL_API_KEY=

# DuckDB warehouse path
DUCKDB_PATH=data/warehouse.duckdb
